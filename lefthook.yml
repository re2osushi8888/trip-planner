pre-commit:
  parallel: true
  commands:
    check-fix:
      run: vp check --fix
    ls-lint:
      run: pnpm ls-lint
    type-check:
      run: pnpm type-check

commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}
