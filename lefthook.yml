pre-commit:
  parallel: true
  commands:
    lint:
      run: pnpm lint
    format:
      run: vp fmt --check
    ls-lint:
      run: pnpm ls-lint
    type-check:
      run: pnpm type-check

commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}
